/**
 * Uniform stroke thinning for map vectors in `StratMapViewer` (territory outline,
 * overlays, ropes, spawns, label halos) — matches the ~50% thinner blueprint intent.
 */
export const MAP_VIEW_VECTOR_STROKE_SCALE = 0.5;
